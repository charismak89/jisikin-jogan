# 예측 캘리브레이션

## 왜 만들었나
8회차까지 시초가 구간 적중률이 낮았고, 원인은 운이 아니라 **재현되는 편향**이었다.
방향은 7회 중 4회 맞았는데(57%), 실제 시초가가 예상 구간 안에 들어온 것은 6회 중 1회(16.7%)뿐이다.
다섯 회 연속으로 움직임의 크기를 실제보다 작게 잡았다. 이건 분산이 아니라 편향이므로 규칙으로 교정된다.

## 지금 상태 — 데이터만 쌓는 단계
`calibration.json` 의 `rule.applied` 는 **false** 다.
기록이 20건이 될 때까지는 예측 방식을 바꾸지 않고 기록만 쌓는다.
8건으로 80퍼센타일을 뽑으면 그 값 자체가 노이즈다.

## 쌓인 뒤 적용할 규칙
```
구간 반폭(%) = 최근 10회 abs(gap_pct) 의 80퍼센타일
```
2026-09-01 기준 참고값 — abs(gap%) 중앙값 1.35 / 80퍼센타일 2.54 / 최대 2.76.
같은 기간 실제 사용한 평균 구간폭은 1.32%p(반폭 0.66)였다. 약 4배 좁게 잡아 온 셈이다.

## 채점 기준
단일 지표 `적중/빗나감` 은 게임된다 — 구간만 넓히면 100%가 나온다.
그래서 두 개를 함께 본다.

| 지표 | 목표 | 뜻 |
|---|---|---|
| coverage | 0.80 | 실제 시초가가 구간 안에 들어온 비율 |
| 평균 band_width_pct | 낮을수록 좋음 | 구간이 얼마나 좁은가 |

coverage 100%는 성공이 아니라 구간이 헐렁하다는 뜻이다.

## 규칙 변경 절차
- 변경은 **금요일 마감 후에만**. 회차 안에서 규칙을 고치지 않는다.
- 한 번에 **한 변수만** 바꾼다.
- 바꾸면 `rule.history` 에 `{date, changed, from, to, why}` 를 남긴다.
- 변경 후 10회차 coverage 가 직전 10회차보다 낮으면 직전 규칙으로 롤백한다.

## 왜 회차마다 고치면 안 되나
규칙이 매일 달라지면 어떤 변경이 효과가 있었는지 영영 분리되지 않는다. 채점 자체가 의미를 잃는다.
규칙 동결이 자기개선의 전제 조건이다.

## 운영 경로
예약 세션은 저장소에 직접 쓰지 못한다.
회차가 `index.html` 과 `calibration.json` 을 함께 내려주고, `publish.bat` 이 둘 다 집어 올린다.
`publish.ps1` 은 새 파일의 records 수가 기존보다 적으면 덮어쓰지 않는다.

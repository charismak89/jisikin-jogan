# 예측 캘리브레이션

## 왜 만들었나
8회차까지 시초가 구간 적중률이 낮았고, 원인은 운이 아니라 **재현되는 편향**이었다.
다섯 회 연속으로 움직임의 크기를 실제보다 작게 잡았다. 이건 분산이 아니라 편향이므로 규칙으로 교정된다.

## 2026-09-14 기준 현황 (17회차, 09-14 회차 채점 포함)
- 구간 커버리지 56.3% (9/16) · 방향 적중 58.8% · 판정(방향+구간) 적중 35.3% (6/17) · 평균 구간폭 2.10%p
- |갭| 중앙 1.33% / 80퍼센타일 3.08% / 최대 3.34%. 17일 중 11일이 갭하락.
- 구간 중심 오차(실제 갭 − 구간 중심) 중앙 0.94%p / 80퍼센타일 1.80%p
- 콜 종류별 — 갭하락 5회: 부호 5/5 · 갭상승 5회: 부호 3/5 (08-26, 09-14 실패) · 보합류 7회: ±0.5% 안 2회
- 09-14 회차는 EWY 시사 갭 +5.63% 를 근거로 +1.16~+4.63% 를 냈고 실제는 −3.14%. 같은 시점 야간선물은 +0.97% 였다. 주말 뉴스는 어느 가격에도 없었다.

## 지금 상태 — 데이터만 쌓는 단계
`calibration.json` 의 `rule.applied` 는 **false** 다. `rule.center_signal` 도 비어 있다.
기록이 20건이 될 때까지는 예측 방식을 바꾸지 않고 기록만 쌓는다.
v2(2026-09-15)부터 기록에 `signals`(야간선물·EWY 시사·ADR 시사·뉴스 플래그)·`sign_hit`·`center_pct`·`center_err_pct` 가 붙는다. 이건 추가 필드일 뿐 예측 방식이 아니다.

## 채점 규칙 (score.py 에 그대로 구현돼 있다)
- 방향 판정: 실제 갭이 +0.5% 초과면 갭상승, −0.5% 미만이면 갭하락, 그 사이는 보합
- `hit` = 방향이 맞고 **동시에** 실제 시초가가 [low, high] 안. 그 외 전부 `miss`
- `in_range` 만 따로 세어 커버리지를 낸다. `sign_hit` 는 구간 중심 부호와 실제 갭 부호가 같은지 (참고용)
- 같은 날짜는 두 번 기록하지 않는다. `rule` 블록은 회차가 절대 손대지 않는다.

기존 기록의 불일치 하나 — 09-01 회차(보합 전망, 실제 −0.52%)는 hit 로 기록돼 있는데 ±0.5% 규칙을 그대로 적용하면 miss 다. 09-08(+0.72%)은 같은 규칙으로 miss 로 기록됐다. 기록은 고치지 않았다. 고칠지는 사람이 정한다(고치면 `flag` 를 남긴다).

## 쌓인 뒤 적용할 규칙 — 두 후보
```
후보 1 (원안): 구간 반폭(%) = 최근 10회 abs(gap_pct) 의 80퍼센타일
후보 2 (v2안): 구간 중심 = 야간선물 등락률(k200n_pct), 반폭 = 최근 10회 |gap_pct − k200n_pct| 의 80퍼센타일
```
후보 1 을 지금 적용하면 반폭 3.1% = 폭 6.3%p 가 나온다. 커버리지는 오르지만 읽는 사람에게 쓸모가 없다.
후보 2 는 야간선물이 시초가를 얼마나 설명하는지에 달렸다. `signals.k200n_pct` 가 10회차 쌓이면 잔차 분포로 판단한다.

## 켜는 방법 (금요일에 사람이)
1. `calibration.json` 의 `rule` 에 `"center_signal": "k200n"`, `"half_width_pct": <반폭 %>` 를 쓴다. 반폭은 잔차 80퍼센타일, 데이터가 없으면 1.5 로 시작한다.
2. `rule.history` 에 `{date, changed, from, to, why}` 를 남기고 `rule.applied` 를 true 로 바꾼다.
3. 다음 회차부터 `score.py` 가 "규칙 적용 — 구간 중심 = 야간선물 …" 줄과 구간을 내고, 회차는 그 값을 그대로 `forecast` 에 쓴다.
4. 끄려면 `center_signal` 을 null 로 되돌린다.

## 채점 기준
단일 지표 `적중/빗나감` 은 게임된다 — 구간만 넓히면 100%가 나온다.
그래서 두 개를 함께 본다.

| 지표 | 목표 | 뜻 |
|---|---|---|
| coverage | 0.80 | 실제 시초가가 구간 안에 들어온 비율 |
| 평균 band_width_pct | 낮을수록 좋음 | 구간이 얼마나 좁은가 |
| center_err_pct 중앙값 | 낮을수록 좋음 | 구간 중심을 얼마나 맞게 잡았는가 (v2 추가) |

coverage 100%는 성공이 아니라 구간이 헐렁하다는 뜻이다.

## 규칙 변경 절차
- 변경은 **금요일 마감 후에만**. 회차 안에서 규칙을 고치지 않는다.
- 한 번에 **한 변수만** 바꾼다.
- 바꾸면 `rule.history` 에 `{date, changed, from, to, why}` 를 남긴다.
- 변경 후 10회차 coverage 가 직전 10회차보다 낮으면 직전 규칙으로 롤백한다.

## 왜 회차마다 고치면 안 되나
규칙이 매일 달라지면 어떤 변경이 효과가 있었는지 영영 분리되지 않는다. 채점 자체가 의미를 잃는다.
규칙 동결이 자기개선의 전제 조건이다.

## 운영 경로 (v2)
회차가 `score.py` 로 직전 전망을 채점해 `calibration.json` 에 1건 append 하고, `render.py` 가 `out/` 에 세 파일을 만든다.
자동 발행(`publish.py --push`)이 되면 저장소에 바로 들어가고, 안 되면 세 파일을 내려받아 `publish.bat` 으로 올린다.
`publish.py` 와 `publish.ps1` 둘 다 새 파일의 records 수가 기존보다 적으면 덮어쓰지 않는다.

# 매일 아침 갱신하는 법

## 방법 A — publish.bat 더블클릭 (권장)

1. 08:00에 Claude가 보낸 `index.html` 을 **다운로드 폴더에 저장**
2. 리포 폴더의 **`publish.bat` 더블클릭**
3. 검은 창이 뜨고 알아서 처리합니다. `발행 완료!` 가 나오면 끝

스크립트가 대신 해주는 일:

- 어제 `index.html` 을 `archive/<어제날짜>.html` 로 복사하고 CSS 경로 수정
- `archive/index.html` 목록에 회차 한 줄 추가 (호수 자동 계산)
- 새 파일로 교체
- 커밋 + 푸시

안전장치도 들어 있습니다. 파일 발행일이 오늘이 아니면 **경고 후 물어보고**,
같은 날짜를 다시 발행하면 아카이브를 덮어쓰지 않습니다.

> 처음 실행할 때 "이 앱이 PC를 변경하도록 허용" 같은 창이 뜨면 허용하세요.
> `publish.bat` 을 마우스 우클릭 → 보내기 → 바탕 화면에 바로 가기 만들기 를 해두면 편합니다.

## 방법 B — VS Code에서 직접 (스크립트가 안 될 때)

1. `index.html` 을 복사해서 `archive` 폴더에 붙여넣고 이름을 `2026-08-21.html` 처럼 그날 날짜로 변경
2. 그 파일에서 `href="style.css"` → `href="../style.css"` 로 수정
3. `archive/index.html` 의 `<!-- ARCHIVE_LIST_START -->` 아래에 한 줄 추가:
   `<a class="item" href="./2026-08-21.html">2026년 8월 21일 (금)<span>제 1 호</span></a>`
4. 받은 `index.html` 을 폴더에 덮어쓰기
5. VS Code 왼쪽 **가지 모양 아이콘** → 메시지 입력 → **커밋** → **변경 내용 동기화**

---

## 파일 설명

| 파일 | 설명 | 손대는 빈도 |
|---|---|---|
| `index.html` | 오늘자 대시보드 | 매일 교체 |
| `archive/*.html` | 지난 회차 | 매일 1개 추가 |
| `archive/index.html` | 회차 목록 | 매일 1줄 추가 |
| `style.css` | 디자인 전체 | 거의 안 건드림 |
| `template.html` | 대시보드 뼈대 | 구조 바꿀 때만 |
| `publish.bat` / `publish.ps1` | 발행 자동화 | 안 건드림 |
| `robots.txt` | 검색엔진 차단 | 안 건드림 |

`style.css` 하나만 고치면 **과거 회차까지 전부** 디자인이 같이 바뀝니다.

# 매일 아침 갱신하는 법

08:00에 Claude가 보내주는 `index.html` 을 받아서 아래 순서로 반영합니다.

## 1. 어제 것을 아카이브로 넘기기

VS Code에서 `index.html` 을 열고, 위쪽 `<meta name="generated" content="2026-08-21T...">` 의
**날짜를 확인**합니다. 그 날짜로 archive에 복사본을 만듭니다.

- `index.html` 우클릭 → **복사**
- `archive` 폴더 우클릭 → **붙여넣기**
- 붙여넣은 파일 이름을 `2026-08-21.html` 처럼 그 날짜로 변경
- 그 파일을 열어서 `href="style.css"` 를 `href="../style.css"` 로 한 글자만 수정

## 2. 아카이브 목록에 한 줄 추가

`archive/index.html` 을 열고 `<!-- ARCHIVE_LIST_START -->` 바로 아래에 붙여넣기:

```html
<a class="item" href="./2026-08-21.html">2026년 8월 21일 (금)<span>제 1 호</span></a>
```

날짜와 호수만 바꿔주면 됩니다. 최신 회차가 맨 위로 갑니다.

## 3. 새 파일로 교체

받은 `index.html` 을 폴더에 덮어씁니다. (같은 이름이니 "덮어쓰기" 선택)

## 4. 올리기

VS Code 왼쪽 **가지 모양 아이콘(소스 제어)** →
메시지에 `brief: 8월 24일자` 처럼 적고 → **커밋** → **변경 내용 동기화**

1~2분 뒤 사이트에 반영됩니다.

---

## 파일 설명

| 파일 | 설명 | 손대는 빈도 |
|---|---|---|
| `index.html` | 오늘자 대시보드 | 매일 교체 |
| `archive/*.html` | 지난 회차 | 매일 1개 추가 |
| `archive/index.html` | 회차 목록 | 매일 1줄 추가 |
| `style.css` | 디자인 전체 | 거의 안 건드림 |
| `template.html` | 대시보드 뼈대 | 구조 바꿀 때만 |
| `robots.txt` | 검색엔진 차단 | 안 건드림 |

`style.css` 하나만 고치면 **과거 회차까지 전부** 디자인이 같이 바뀝니다.
